
#MySQL Database Details

spring.datasource.url = jdbc:mysql://127.0.0.1:3306/springdbschema?useSSL=false
spring.datasource.username = root
spring.datasource.password = root
spring.datasource.driver-class-name=com.mysql.jdbc.Driver


# Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
#logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

# SQL Utility Changes
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


#System Server Port
server.port=8081

#LOGGING
logging.level.org.springframework.web= DEBUG

#Jackson
spring.jackson.serialization.indent_output=true


# ---------------------------------Generate Log in File---------------------------------------
logging.level.root=DEBUG
logging.level.com.example.SpringMongoDB.Driver=DEBUG
logging.level.org.hibernate= DEBUG
logging.level.net.guides = DEBUG

#Logging Pattern for files
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

#Logging Pattern for console

logging.file.name=Logs/application.log
logging.file.max-size=20MB

# ---------------------------Spring Banner Information-------------------------------------
spring.banner.location=classpath:banner-dev
#spring.main.banner-mode=off
spring.banner.image.location = classpath:logo.png
# ------------------------------------------------------------------------------------------

#Color of Log Output
spring.output.ansi.enabled=ALWAYS

#Fix Output
spring.jackson.serialization.fail-on-empty-beans=false

# Manage Health Actuators
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*

# Batch Configuration
spring.batch.job.enabled=false;
spring.batch.initialize-schema=never

# Java Messaging Service (Active MQ)
#spring.activemq.broker.url=http://127.0.0.1:8161/
#spring.activemq.broker.url=vm://localhost:61616
spring.activemq.broker-url=tcp://localhost:61616
spring.activemq.user=admin
spring.activemq.password=admin
spring.activemq.packages.trust-all=true 

# File Upload Constants
uploadDir=D://Eclipse_Workspace//FileRepo//To//
downloadPath=D://Eclipse_Workspace//FileRepo//DownloadFiles//

# Password Generator
SECRET_KEY=QJkT34LLu56PNAJGAHJGL227G
PROGRAM.SECRET_KEY=QJkT34LLu56PNAJGAHJGL227G